const chai = require("chai");
describe("Testing WebXDAO Extension", () => {
  describe("Sample test", () => {
    it("should return a 1", () => {
      sum = 1;
      chai.expect(1).equal(sum);
    });
  });
});
